using _09_Inheritance.Entity;

    // Inheritance
    // makes our code reusable
    // keep us from repeating ourselves

    Cat cat = new Cat();

    Console.WriteLine(cat.IsMammal);
    Console.WriteLine(cat.DietType);

    TabbyCat tabbyCat = new TabbyCat();
    
